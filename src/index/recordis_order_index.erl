-module(recordis_order_index).
-author("yimo").

%% API
-export([]).
%% 仅限int，float列

%%create(Record, Column) ->
%%    ok.
%%
%%delete(Record, Column) ->
%%    ok.
%%
%%update(Record, Column, Value) ->
%%    ok.